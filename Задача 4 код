#include <iostream>
#include <limits>
#include <cmath>
/**
 * @brief считывает значение с клавиатуры с проверкой ввода
 * @return введенное значение
 */
double getValue();
/**
 * @brief проверяет существование введенного шага
 * @param step - шаг
 */
void checkStep(const double step);
/**
 * @brief расчитывает функцию y(x)
 * @return расчитанное значение
 */
double getY(const double x);

/**
 * @brief точка входа в программу
 * @return 0 если программа выполнена корректно, иначе 1
 */
using namespace std;
int main()
{
    cout << "Enter xn: ";
    double xstart = getValue();
    cout << "Enter xk: ";
    double xend = getValue();
    cout << "Enter step: ";
    double step = getValue();
    checkStep(step);
    for (double x = xstart; x < xend + step; x += step)

        cout << "x = " << x << " y = " << getY(x) << endl;


    return 0;
}

double getValue()
{
    double value = 0;
    cin >> value;
    if (cin.fail())
    {
        cout << "Incorrect value" << endl;
        abort();
    }
    return value;
}

void checkStep(const double step)
{
    if (step <= 0)
    {
        cout << "Error value" << endl;
        abort();
    }
}

double getY(const double x)
{
    return sin(log (x))-cos(log (x))+2*log (x);
}
