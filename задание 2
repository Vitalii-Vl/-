#include <iostream>
#include <stdlib.h>
using namespace std;
/**
 * @brief считывает значение с клавиатуры с проверкой ввода
 * @return введенное значение
 */
double getValue();
 
 /**
 * @brief рассчитывает сумму чисел
 * @param a - первое число
 * @param b - второе число
 */
 double sum (const double a, const double b);
 
 /**
 * @brief рассчитывает разность чисел
 * @param a - первое число
 * @param b - второе число
 */
 double dif (const double a, const double b);
 /**
 * @brief рассчитывает произведение чисел
 * @param a - первое число
 * @param b - второе число
 */
 double pro (const double a, const double b);
 /**
 * @brief проверяет существование деления на 0
 * @param b - знаменатель
 */
 void checkExist(const double a,const double b);
 /**
 * @brief рассчитывает частное чисел
 * @param a - первое число
 * @param b - второе число
 */
 
 double pri (const double a, const double b);
 
 int main()
 {
  cout<<"Введите первое число ";
  double a = getValue();
  cout<<"Введите второе число ";
  double b = getValue();
  cout<<"Сумма чисел: "<<sum(a,b)<<endl;
  cout<<"Разность чисел: "<<dif(a,b)<<endl;
  cout<<"Произведение чисел: "<<pro(a,b)<<endl;
  checkExist(a, b);
  cout<<"Частное чисел: "<<pri(a,b)<<endl;
  return 0;
 }
 double getValue()
{
    double value;
    cin>>value;
    if (cin.fail())
    {
        cout<<"Incorrect value"<<endl;
        abort();
    }
    return value;
}


double sum (const double a, const double b)
{
    return a + b;
}

double dif (const double a, const double b)
{
    return a - b;
}
double pro (const double a, const double b)
{
    return a*b;
}
void checkExist(const double a,const double b)
{
    if (b == 0)
    {
        cout<<"Error"<<endl;
        abort();
    }
}
